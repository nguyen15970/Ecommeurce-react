import React from 'react';
import PropTypes from 'prop-types';
import Navbar from '../components/Header/Navbar';

Cart.propTypes = {
    
};

function Cart(props) {
    return (
        <div>
            <Navbar/>
        </div>
    );
}

export default Cart;